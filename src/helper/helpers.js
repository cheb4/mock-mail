const logToConsole = (obj, name) => {
  console.log(`+-${name}-----------------------${name}-+`);
  console.log(`${name} -->  ${obj}`);
  console.log(`+-${name}-----------------------${name}-+`);
};
export default logToConsole;
